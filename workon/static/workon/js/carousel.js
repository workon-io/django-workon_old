$(document).ready(function()
{
    $('body > header .carousel').carousel({
        fullWidth: true
    });
    $('main .carousel').carousel({
        // shift: 10,
        // dist : -15,
        // padding: 20,
        // interval: 200
    });
});